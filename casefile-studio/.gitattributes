# cmd.exe can mis-parse labels and parenthesised blocks in a .bat file that
# has LF-only line endings, so force CRLF on checkout regardless of platform.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

*.py  text eol=lf
*.md  text eol=lf
*.txt text eol=lf
